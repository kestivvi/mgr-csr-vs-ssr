version: '3.8'
services:
  telegraf:
    image: telegraf:1.30
    container_name: load-generator-telegraf
    restart: unless-stopped
    network_mode: "host"
    volumes:
      - ./telegraf.conf:/etc/telegraf/telegraf.conf:ro 