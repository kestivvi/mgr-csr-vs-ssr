services:
  svelte-app:
    build: .
    image: ssr-svelte-kit-app
    restart: unless-stopped
    networks:
      - app-net

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    image: ssr-svelte-kit-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "8060:8060"
    depends_on:
      - svelte-app
    networks:
      - app-net

networks:
  app-net:
    driver: bridge