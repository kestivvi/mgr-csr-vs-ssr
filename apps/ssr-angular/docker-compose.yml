services:
  angular-app:
    build: .
    image: ssr-angular-app
    restart: unless-stopped
    networks:
      - app-net

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "8060:8060"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - /var/log/ssr-angular-nginx:/var/log/nginx
    depends_on:
      - angular-app
    networks:
      - app-net

networks:
  app-net:
    driver: bridge

