services:
  angular-app:
    build: .
    image: ssr-angular-app
    restart: unless-stopped
    networks:
      - app-net

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    image: ssr-angular-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "8060:8060"
    depends_on:
      - angular-app
    networks:
      - app-net

networks:
  app-net:
    driver: bridge
