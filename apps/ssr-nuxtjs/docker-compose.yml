services:
  nuxt-app:
    build: .
    image: ssr-nuxtjs-app
    restart: unless-stopped
    networks:
      - app-net

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    image: ssr-nuxtjs-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "8060:8060"
    depends_on:
      - nuxt-app
    networks:
      - app-net

networks:
  app-net:
    driver: bridge
