services:
  astro-app-bun:
    build:
      context: .
      target: runner
    image: ssr-astro-bun-app
    restart: unless-stopped
    networks:
      - app-net

  nginx:
    build:
      context: .
      target: nginx
    image: ssr-astro-bun-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "8060:8060"
    depends_on:
      - astro-app-bun
    networks:
      - app-net

networks:
  app-net:
    driver: bridge
