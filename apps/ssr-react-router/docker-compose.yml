services:
  react-router-app:
    build: .
    image: ssr-react-router-app
    restart: unless-stopped
    networks:
      - app-net

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    image: ssr-react-router-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "8060:8060"
    depends_on:
      - react-router-app
    networks:
      - app-net

networks:
  app-net:
    driver: bridge
