services:
  fresh-app:
    build: .
    image: ssr-fresh-app
    restart: unless-stopped
    networks:
      - app-net

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    image: ssr-fresh-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "8060:8060"
    depends_on:
      - fresh-app
    networks:
      - app-net

networks:
  app-net:
    driver: bridge
