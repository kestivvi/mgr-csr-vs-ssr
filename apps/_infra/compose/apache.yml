services:
  apache:
    build:
      context: ../../${APP_DIR}
    image: mgr-${APP_ID}-apache
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-80}:80"
      - "443:443"
      - "8060:8060"
      - "9117:9117"
    networks:
      - app-net

networks:
  app-net:
    driver: bridge
