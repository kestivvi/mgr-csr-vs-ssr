# Infrastructure Configuration Template
aws_region: ap-south-1
key_name: MGR-EXAMPLE
my_ip: 0.0.0.0/0

# Instance Types
app_server_instance_type: c8g.medium
load_generator_instance_type: c8g.2xlarge
monitoring_server_instance_type: t4g.small

# Technologies to deploy
# Note: This block is now auto-populated by the orchestrator at runtime via Subject Autodiscovery.
# You only need to add entries here if you want to override defaults (e.g. description or specific settings).
technologies: {}
