# Infrastructure Configuration Template
aws_region: ap-south-1
key_name: MGR-M
my_ip: 46.205.203.156/32

# Instance Types
application_server_instance_type: c8g.medium
load_generator_instance_type: c8g.2xlarge
monitoring_host_instance_type: t4g.small

# application_server_instance_type: t4g.micro
# load_generator_instance_type: t4g.micro
# monitoring_host_instance_type: t4g.micro

# Applications to deploy
# Note: This block is now auto-populated by the orchestrator at runtime via Application Autodiscovery.
# You only need to add entries here if you want to override defaults (e.g. description or specific settings).
technologies: {}
