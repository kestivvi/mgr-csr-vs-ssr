services:
  nginx:
    build:
      context: ../../${SUBJECT_DIR}
      target: ${BUILD_TARGET:-nginx}
      additional_contexts:
        infra: ../nginx
    image: mgr-${SUBJECT_ID}-nginx
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-80}:80"
      - "443:443"
      - "8060:8060"
    networks:
      - subject-net
    environment:
      - NGINX_TEMPLATE=/etc/nginx/conf.d/default.csr.conf.template

networks:
  subject-net:
    driver: bridge
